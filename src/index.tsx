import * as React from 'react';
import * as ReactDOM from 'react-dom';
import LazyBox from './components/ImageGallery';
 
ReactDOM.render(
  <LazyBox  />,
  document.getElementById('root') as HTMLElement
);
